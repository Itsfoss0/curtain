<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Event Registration Confirmation</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        line-height: 1.6;
        color: #333333;
        margin: 0;
        padding: 0;
      }

      .container {
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
      }

      .header {
        background-color: #3498db;
        padding: 20px;
        text-align: center;
        color: white;
      }

      .content {
        padding: 20px;
        background-color: #ffffff;
      }

      .event-details {
        background-color: #f9f9f9;
        padding: 15px;
        border-radius: 4px;
        margin: 20px 0;
      }

      .ticket-info {
        border: 1px dashed #cccccc;
        padding: 15px;
        margin: 20px 0;
      }

      .footer {
        padding: 20px;
        text-align: center;
        font-size: 12px;
        color: #888888;
        background-color: #f9f9f9;
      }

      .button {
        display: inline-block;
        padding: 10px 20px;
        background-color: #3498db;
        color: white;
        text-decoration: none;
        border-radius: 4px;
        margin: 20px 0;
      }
    </style>
  </head>

  <body>
    <div class="container">
      <div class="header">
        <h1>Your Registration is Confirmed!</h1>
      </div>
      <div class="content">
        <p>Hello {{name}},</p>
        <p>
          Thank you for registering for {{eventTitle}}. Your registration has
          been confirmed!
        </p>

        <div class="event-details">
          <h2>Event Details</h2>
          <p><strong>Event:</strong> {{eventTitle}}</p>
          <p><strong>Date:</strong> {{eventDate}}</p>
          <p><strong>Time:</strong> {{eventTime}}</p>
          <p><strong>Location:</strong> {{eventLocation}}</p>
        </div>

        <div class="ticket-info">
          <h2>Ticket Information</h2>
          <p><strong>Order Number:</strong> {{orderNumber}}</p>
          <p><strong>Ticket Type:</strong> {{ticketType}}</p>
          <p><strong>Quantity:</strong> {{ticketQuantity}}</p>
          <p><strong>Price per Ticket:</strong> ${{ticketPrice}}</p>
          <p><strong>Total Amount:</strong> ${{totalAmount}}</p>
        </div>

        <p style="text-align: center">
          <a href="{{ticketDownloadLink}}" class="button">Download Tickets</a>
        </p>

        <p>
          <strong>Add to Calendar:</strong>
          <a href="{{googleCalendarLink}}">Google</a> |
          <a href="{{outlookCalendarLink}}">Outlook</a> |
          <a href="{{icsFileLink}}">iCal</a>
        </p>

        <p>
          If you have any questions or need to make changes to your
          registration, please contact us at support@scheduleflow.io .
        </p>

        <p>We look forward to seeing you at the event!</p>

        <p>
          Best regards,<br />
          The Scheduleflow Team
        </p>
      </div>
      <div class="footer">
        <p>
          This email was sent to {{email}} regarding your registration for
          {{eventTitle}}.
        </p>
        <p>© {{currentYear}} {{companyName}}. All rights reserved.</p>
        <p>
          <a href="{{privacyPolicyLink}}">Privacy Policy</a> |
          <a href="{{termsOfServiceLink}}">Terms of Service</a>
        </p>
      </div>
    </div>
  </body>
</html>
